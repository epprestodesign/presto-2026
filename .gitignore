node_modules
dist
dist-netlify
storybook-static
*.log
.DS_Store
.vite

# Secrets — never commit the Unsplash key
.env
.env.local
.env.*.local

# Design reference screenshots — kept local only, not tracked
references/

# Local design doc + audit — never commit
07072026/
